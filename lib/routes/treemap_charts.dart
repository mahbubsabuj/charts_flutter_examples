import 'package:flutter/material.dart';

class TreeMapCharts extends StatelessWidget {
  const TreeMapCharts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("TreeMap Charts")),
    );
  }
}
